/* ===========================================================================
   Site configuration (safe to edit by hand — no rebuild needed for these).
   =========================================================================== */
window.PDFTOOLS_CONFIG = {
  // Cloudflare Worker URL that proxies CloudConvert (keeps the API key secret).
  convertEndpoint: "https://pdf-convert.toofan-tempest59.workers.dev",
};
